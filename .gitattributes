# Normalize all text files to LF in the repo and working tree so Windows
# CRLF conversion stops showing every file as modified.
* text=auto eol=lf

# Binary assets: never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary
*.hbc binary
